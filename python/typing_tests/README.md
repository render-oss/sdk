# Typing tests

These files are never executed. They are checked by mypy, and mypy failing
*is* the test failure. They assert that our typing is correct.

You can run them with `uv run mypy typing_tests`
