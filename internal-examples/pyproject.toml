[project]
name = "internal-examples"
version = "0.1.0"
description = ""
authors = [
    {name = "Mark Benjamin",email = "mark@render.com"}
]
requires-python = ">=3.13,<4.0"
dependencies = [
    "render-sdk (>=0.1.3,<0.4.0)"
]

[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
